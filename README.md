# Dotfiles repo (intended to be used with the GNU stow tool)

To dry run restowing of everything, run from this directory:
```sh
stow -n -v -R [^.]*/
```

To restow everything, run from this directory:
```sh
stow -v -R [^.]*/
```
